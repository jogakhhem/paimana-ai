services:
  backend:
    build: .
    container_name: paimana-backend
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: paimana-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped